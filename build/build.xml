<?xml version="1.0" encoding="UTF-8"?>

<project name="Rueckgrat" default="build" basedir=".">

 <target name="prepstructure" description="Prepare the structure of the codebase">
  <delete dir="${basedir}/doc"/>
  <delete dir="${basedir}/build/logs"/>
  <delete dir="${basedir}/build/testresults"/>
  <mkdir dir="${basedir}/doc"/>
  <mkdir dir="${basedir}/build/logs"/>
  <mkdir dir="${basedir}/build/testresults"/>
 </target>

 <target name="phpcs" description="Generate checkstyle.xml using PHP_CodeSniffer">
  <exec executable="phpcs">
   <arg line="--report=checkstyle --report-file=${basedir}/build/logs/checkstyle.xml --standard=PSR ${basedir}/../src/" />
  </exec>
 </target>

 <target name="phpdoc" description="Generate API documentation">
 <exec executable="phpdoc">
  <arg line="-d ${basedir}/../src -t ${basedir}/../doc" />
 </exec>
</target>

 <target name="build" depends="prepstructure,phpcs,phpdoc"/>

</project>